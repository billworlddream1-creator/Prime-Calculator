import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.primecalculator.app',
  appName: 'Prime Calculator',
  webDir: 'dist'
};

export default config;
